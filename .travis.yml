language: python
python:
  - "2.7"
# command to install dependencies
addons:
  apt:
    packages:
    - libpcre3-dev
    - python-smbus
    - python-dev
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
