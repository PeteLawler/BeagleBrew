[Unit]
Description=BeagleBrew service
After=network.service

[Service]
Type=simple
ExecStart=/usr/bin/python3 -u INSTALL_LOCATION/beaglebrew.py 2>&1
WorkingDirectory=INSTALL_LOCATION

[Install]
WantedBy=multi-user.target
